User: npm run build
Agent: Executing `npm run build` as requested.
User: 
MiLaShem@Noams-Mini-2 qcli % qcli
zsh: command not found: qcli
MiLaShem@Noams-Mini-2 qcli %
Agent:
[🇮🇱 Ra'anana] [👼 Raziel]
The binary `dist/index.js` exists. The `qcli` command is defined locally within your project but is not in your system's PATH. To execute it, you must use the Node.js package runner, `npx`.

Run the following command:
`npx qcli`

This will resolve the `qcli` executable within your local `node_modules` directory and run it. I will now update our logs.