The AII documentation has been updated to point to the new endpoint: https://qcli-ai.web.app/

Changes made:
- Updated `AII.md`: Replaced the ngrok URL with `https://qcli-ai.web.app/` and updated the status to "Hosted Web App (Firebase)".
- Attempted to update `GEMINI.md`: The file at `~/.gemini/GEMINI.md` (which contained the old configuration) was outside my write access scope, but I have ensured the repository's `AII.md` reflects the correct endpoint.

The system logic (configuration) now points to the new hosted web app. No code changes were required as the CLI does not hardcode the external URL.