***************
*** 29,35 ****
          ws.on('message', async (data) => {
              try {
                  const message = JSON.parse(data.toString());
-                 console.log(chalk.gray(`[Q-Local] Received: ${message.type}`));
  
                  switch (message.type) {
                      case 'PING':
--- 29,35 ----
          ws.on('message', async (data) => {
              try {
                  const message = JSON.parse(data.toString());
+                 console.log(chalk.gray(`[Q-Local] Received: ${message.type} - ${message.payload?.substring(0, 30)}...`));
  
                  switch (message.type) {
                      case 'PING':
